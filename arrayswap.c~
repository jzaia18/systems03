#include stdlib.h
#include time.h


int main() {
  
  int arr[10];

  int i = 0;
  for(; i<10; i++) {
    srand(time(NULL));
    arr[i] = rand()
  }

  arr[9] = 0;

  for(i=0; i<10; i++)
    printf("arr[%d]: %d", i, arr[i]);
  
  return 0
}
